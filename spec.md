你是一位精通前端互動視覺化與使用者體驗（UX）的專家。
我現在需要為一個工作坊開發一個「未來輪（Futures Wheel）」的極簡 Prototype。這個工具必須滿足「免安裝、跨平台（桌面、手機、平板 iOS/Android）、點開即用」的特徵。

請幫我生成一個「單一 HTML 檔案」（包含所有 HTML, CSS, JavaScript，不需任何 npm 安裝與編譯環境），讓我可以直接在瀏覽器中打開運行。

### 1. 技術選型要求
- 請使用原生 JavaScript 搭配 Vis.js Network 庫（透過 CDN 引入：https://cdnjs.cloudflare.com/ajax/libs/vis-network/9.1.9/vis-network.min.css 與 .min.js），因為它對行動端手勢（雙指縮放、單指拖曳）與動態連線有極佳的原生支援，且輕量、載入速度快。
- 介面樣式請使用 Tailwind CSS（透過 CDN 引入），並設計成現代、乾淨、適合工作坊的極簡風格。

### 2. 功能需求
- **初始輸入介面：**
  - 頁面頂端或側邊有一個簡單的輸入區域。
  - 包含一個「核心主題」輸入欄（預設值：生成式 AI 普及）。
  - 三個「直接影響」輸入欄（預設值：初級工程師轉型、軟體開發成本下降 50%、資安風險增加）。
  - 一個「生成未來輪」的按鈕。
- **自動圓圈佈局（Auto-Layout）：**
  - 當點擊按鈕時，在下方的畫布中央生成「核心主題圓圈」（樣式請用深色或顯眼顏色區分）。
  - 以核心為中心，利用圓周公式自動將三個「直接影響圓圈」以 120 度的夾角、適當的半徑距離（如 150-200px）圍繞在核心四周，並自動連線。
- **工作坊互動功能：**
  - **自由拖動：** 所有圓圈（節點）都可以用滑鼠（電腦）或手指（手機/平板）自由拖曳定位。
  - **重新連線 / 編輯：** 
    - 畫布需啟用 Vis.js 的編輯模式（Manipulation mode），允許使用者手動刪除連線、或是從 A 圓圈拉出新連線到 B 圓圈。
    - 在畫布旁提供簡易的手動操作提示（例如：「雙擊空白處新增次級影響」、「點擊連線按鈕可重連」）。
- **跨平台與 RWD 優化：**
  - 確保畫布（Canvas）能自動填滿剩餘螢幕空間。
  - 行動端（手機）操作時，字體與圓圈大小要適中，觸控點擊必須靈敏。

請直接給我完整、無刪減、複製即可運行的單一 HTML 原始碼。